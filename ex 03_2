hrs = input("Enter Hours:")
rate = input("enter rate:")
try:
    h = float(hrs)
    r = float(rate)
except:
    print("enter number")
    quit()

if h > 40:
    reg = h * r
    otp = (h - 40.0) * (r * 0.5)
    pay = reg + otp
else:
    pay = h * r
print(" ",pay)
